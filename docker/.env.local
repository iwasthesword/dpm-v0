# =============================================================================
# DPM - Local Testing Environment Variables (for Portainer)
# =============================================================================

# Domain Configuration
DOMAIN=localhost
ACME_EMAIL=test@test.com

# Database (PostgreSQL)
DB_USER=dpm
DB_PASSWORD=testpass123
DB_NAME=dpm

# Redis
REDIS_PASSWORD=redispass123

# JWT Authentication
JWT_SECRET=dev-jwt-secret-minimum-32-characters-long
JWT_REFRESH_SECRET=dev-refresh-secret-minimum-32-chars-long

# MinIO Object Storage
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=miniopass123
STORAGE_BUCKET=dpm-files

# Stripe (disabled for local testing)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Logging
LOG_LEVEL=debug
