# =============================================================================
# DPM - N7 Digital Production Environment Variables
# =============================================================================
# Copy these to Portainer environment variables when deploying
# =============================================================================

# Database (dedicated PostgreSQL for DPM)
DB_USER=dpm
DB_PASSWORD=your-secure-db-password
DB_NAME=dpm

# Redis
REDIS_PASSWORD=your-secure-redis-password

# MinIO (S3-compatible storage)
MINIO_ROOT_USER=dpm-minio-admin
MINIO_ROOT_PASSWORD=your-secure-minio-password
STORAGE_BUCKET=dpm-files

# JWT Secrets (generate with: openssl rand -base64 32)
JWT_SECRET=your-jwt-secret-min-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-min-32-chars

# Optional: Stripe (for payments)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Logging
LOG_LEVEL=info

# =============================================================================
# Default Login Credentials (from seed)
# =============================================================================
# Admin:        admin@demo.dpm.local / Admin@123
# Dentist:      dentist@demo.dpm.local / Dentist@123
# Receptionist: reception@demo.dpm.local / Reception@123
# =============================================================================
