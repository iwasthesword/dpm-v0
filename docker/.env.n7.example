# =============================================================================
# DPM - N7 Digital Production Environment Variables
# =============================================================================

# Domain
DOMAIN=dpm.n7.digital

# Database
DB_USER=dpm
DB_PASSWORD=change-me-secure-password
DB_NAME=dpm

# Redis
REDIS_PASSWORD=change-me-secure-password

# MinIO (S3-compatible storage)
MINIO_ROOT_USER=dpm-admin
MINIO_ROOT_PASSWORD=change-me-secure-password

# JWT Secrets (generate with: openssl rand -base64 32)
JWT_SECRET=change-me-min-32-characters
JWT_REFRESH_SECRET=change-me-min-32-characters

# =============================================================================
# Default Login: admin@demo.dpm.local / Admin@123
# =============================================================================
